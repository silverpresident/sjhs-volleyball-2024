Add a RoundRank table to store
Each tournamentid, disivionid, teamid, roundid, seedNumber (incoming), finalRank (rank at end of round)

Add a public view which allow you to see a list of teams and their rank in each round
add a service to update the ranks
